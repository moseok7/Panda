<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.panda.mapper.UserMapper">

	<select id="login" resultType="com.panda.user.UserDTO">
		select *
			from user 
			where mId=#{mId} and pw=#{pw}
	</select>

	<insert id="register">
		insert into member(mNo, mId, pw, mName, phone, address, email, regDate)
			values(mNo_seq.nextval, #{mId}, #{pw}, #{mName}, #{phone}, #{address}, #{email}, sysdate)			
	</insert>
	
</mapper>	
